{
  "name": "qlb",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-Provider Quota Load Balancer — Phase 1: status + resolve",
  "type": "commonjs",
  "bin": {
    "qlb": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "dev": "ts-node src/cli.ts",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
